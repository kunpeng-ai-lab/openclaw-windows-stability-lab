# 01 问题案例库

案例库记录真实 Windows 稳定性问题。每个案例必须包含环境、症状、证据、根因分类、复用检查项、GEO Agent 映射和上游状态。

第一批案例：

- Feishu 收到消息但 agent dispatch 后 gateway probe timeout。
- update 后隐藏 CMD / `findstr` 窗口弹出。
- Scheduled Task running 但 18789 未监听。
- Node 24 Windows ESM 路径错误。
- 多 gateway 进程残留。

